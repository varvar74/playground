# Angular Open File Dialog

This Angular 17 front-end renders a Windows-style "Open File" dialog that communicates with the companion ASP.NET Core API in `../server`.

## Development

```bash
cd client
npm install
npm start
```

Set the API base URL in `src/environments/environment.ts` if the backend is hosted elsewhere.
